# AGENTS.md — 指数/ETF 量化系统

> 基准设计文档：`指数ETF量化系统设计方案.md`。任何架构/策略决策以该文档为准；本文件只沉淀 Agent 干活时需要的约定，不与设计文档重复细节。
> 当前进度与遗留事项见 `PROGRESS.md`（跨会话接力先读它）。

## 项目定位

指数/ETF 层面的中低频量化系统（趋势跟踪、均值回归、FED 股债利差择时、多资产动量轮动），标的池为 12 只 ETF（见设计文档第三节）。**不做**个股横截面、ETF 折溢价套利、日内高频。

## 工具栈（已定稿，不要擅自替换）

- 数据：AKShare；存储：pandas + Parquet（不引入 DuckDB/polars）
- 信号回测：vectorbt；组合再平衡：bt；报告：QuantStats
- 调参：Optuna + walk-forward；展示：Streamlit + Plotly
- 执行：手动 / 券商条件单（不写程序化下单代码）

## 目录与存储纪律

```text
data/
├── raw/       # 接口返回什么样存什么样，只增不改
└── derived/   # 由 raw 全量重算（read raw → compute → overwrite），可删可重建
```

硬性规则：

1. raw 层**只追加、不回写**；清洗对齐只发生在 derived 层。
2. 列名入库即统一为 `date, open, high, low, close, volume, amount`，date 转 datetime、排序、去重。
3. 文件命名带口径（如 `erp_000300.parquet` vs `erp_ratio_000300.parquet`）；每个文件记录来源接口、复权方式、抓取日期。
4. 中间量同表保留（如存 ERP 时保留 pe、ep、bond_10y）。
5. `metrics_history.parquet` 是唯一允许"每日追加一行"的文件；其余指标结果**不落盘**，仪表盘启动时实时计算 + `st.cache_data` 当天缓存。

## 信号-执行分离（QDII 纪律）

- 信号层一律用干净价格：海外指数（本币）× 汇率；兜底用基金净值。QDII 溢价（曾达 20%+）严禁进入信号计算。
- 执行层用 ETF 前复权市价（`fund_etf_hist_em(adjust="qfq")`，必须前复权）。
- 两层之间用溢价过滤器连接：4 只 QDII（513500/513100/513880/159545）买入前检查溢价率 ≤3%，超限跳过或顺延。

## 执行规范（写回测代码时必须遵守）

1. 511360 短融 ETF **不参与动量排名**，仅作全负时停泊资产；空仓期停泊它（2013 年前用固定年化 2% 日收益填充）。
2. 信号次日开盘成交——严禁信号日收盘价成交的未来函数。
3. 单只 QDII 仓位上限 20–25%。
4. 债券腿 2017 年前用中债国债总财富指数拼接（收益率对齐，不是价格直接接）。

## 验证纪律（改策略/参数时）

1. 第一轮复现一律用原文/研报原参数，不改一个数。
2. 参数取"高原中心"而非"回测最优点"；参数孤峰 = 噪音拟合，放弃该策略。
3. walk-forward 滚动校准 + 最后一年 untouched holdout 只做一次终验。
4. 指数回测通过后必须换真实 ETF 前复权价复测。
5. 通过线：趋势基线相比买入持有最大回撤砍 40%+ 且年化 ≥ 其 70%；均值回归样本外夏普 >1。

## 数据验收（新增/更新数据后跑一遍）

沪深300 ≥5000 交易日；指数与对应 ETF 日涨跌幅偏差 <0.5%；300PE 2007 年 >40、2014 年 ≈8；10Y 国债在 2.5%–5.5% 区间；QVIX 高点对应 2015/2018/2020/2024 危机。

## 指标层契约

每个估值模型 = 一个 Python 函数，输出统一 `MetricResult`：`name / current / unit / sigma / rating(1-5) / color / series / ±1σ带 / desc / direction`。综合层 `aggregate.py` 做 σ 加权出综合分 + 红绿灯。

## 沟通与代码约定

- 全程使用中文交流；代码标识符用英文，注释用中文。
- 变更设计决策（标的池、工具栈、执行规范等）必须先改 `指数ETF量化系统设计方案.md` 并说明理由，再动代码。
- **有新进展必记**：每完成一个步骤、做出重要变更或发现遗留问题，当场更新 `PROGRESS.md`，不要攒着。

## 数据模块（第 1 步已落地）

- 代码：`src/data_module/`（`storage.py` 存储/增量追加/meta 侧车；`sources.py` 数据集注册表；`update.py` CLI；`validate.py` 五查；`bond_splice.py` 债券腿拼接；`tracking_check.py` 跟踪差检验；`net.py` 东财补丁+重试）。
- 环境：`uv sync` 后用 `uv run python -m data_module.update`（全量/增量、断点续传）、`...validate`（五查）、`...bond_splice`、`...tracking_check`。
- 当前网络下东财间歇性不可达：首选源快试 3 次后走新浪/腾讯/中证官网兜底，实际来源以各 `.meta.json` 为准。
