# DESIGN.md — 前端设计规范（CMV 风格，全项目前端定稿）

> 本项目所有前端页面（Streamlit + Plotly）统一遵循本规范。基准参考
> [CurrentMarketValuation.com](https://www.currentmarketvaluation.com/) 的模型页结构，
> 配色与图表纪律结合 dataviz 规范（状态色/序列色/墨色分离）。σ 评估方法见
> `指数ETF量化系统设计方案.md` 第六节；本文件只管"长什么样、怎么写文案"。
> 新增页面或组件必须先对齐本文；改风格 = 先改本文，再动代码（同 AGENTS.md 变更纪律）。

---

## 一、设计原则（从 CMV 提炼，不可违背）

1. **一个模型一页，五件套结构**——当前值大字 → 偏离 σ → 五档红绿灯 → 历史走势 + σ 带 → 白话解释。读者 10 秒内得到"现在是贵是贱、贵多少"。
2. **评级 = 偏离历史常态的标准差数**，不是涨跌预测。所有数字都回答"当前值在自身历史分布的什么位置"。
3. **逆向指标配色翻转**：情绪/恐慌类指标（QVIX、两融等）恐慌 = 绿（机会）、贪婪 = 红（风险）；估值类指标高估 = 红、低估 = 绿。方向由 `MetricResult.direction` 驱动，禁止页面手写。
4. **诚实展示**：无前视（expanding 当时视角）、样本不足明示（"样本不足"，不猜不留空）、兜底数据源明示。
5. **元信息必标**：数据截止日、窗口口径、样本数、均值与 σ——没有这四样的数字不许上页面。

---

## 二、页面骨架（模型页标准区块，自上而下）

| # | 区块 | 实现 | 要点 |
|---|---|---|---|
| 1 | 模型名 + 元信息行 | `st.header` + `st.caption` | 数据截止 · 窗口（起点/月数/均值/σ）· 方法出处链接 |
| 2 | 当前值大字 + 评级徽章 | HTML `st.markdown` | 大字 3.2rem 系统字重 700 + 单位小字；徽章 = 评级色底 + 档名 + σ 偏离 |
| 3 | 五档红绿灯色条 | HTML flex 五格 | 每格：σ 范围小字 + 色块 + 档名；当前档 3px 墨色描边 + "▲ 当前" |
| 4 | 主图 | plotly | 全历史月频走势 + 均值虚线 + ±1σ/±2σ 点线 + 极值区 wash |
| 5 | 副图（估值类标配） | plotly | expanding 评级演进（无前视），±2σ 外着色 |
| 6 | 白话解释 | `st.markdown` 引用块 | 模板见第五节 |
| 7 | 辅证区 | `st.table` × 2 | 双窗口对照 + 历史锚点回验 |
| 8 | 页脚 | `st.caption` | 契约说明（实时计算不落盘）+ 扩展说明 |

侧栏固定：模型选择 → σ 窗口切换（近10年/全历史）→ 方法说明折叠页。

### 回测页标准区块（2026-08-25 增补，首个应用：`app/gem_backtest.py`）

回测结果页与估值模型页并列的第二种页面类型：**只读 derived 落盘结果**（页面不做回测引擎，回测由 CLI 产出到 `derived/backtests/`，`@st.cache_data` 当天缓存）。区块自上而下：

| # | 区块 | 实现 | 要点 |
|---|---|---|---|
| 1 | 策略名 + 元信息行 | `st.header` + `st.caption` | 数据截止 · 回测区间 · 参数（回望/调仓/成本/溢价门）· 方法出处 |
| 2 | 指标摘要 | `st.metric` 行 ×4 + `st.table` | 年化 / 最大回撤 / Calmar / 换仓次数（正负色仅用于超额，涨跌禁用红绿状态色） |
| 3 | 净值对比图 | plotly | 策略 vs 基准（买入持有）vs 其余资产腿，categorical 序列色，多序列开图例 |
| 4 | 回撤图 | plotly | 策略 vs 基准，回撤填充仅 8% wash |
| 5 | 月度收益热力图 | plotly | 年×月，diverging colorscale = categorical 蓝→白→橙（**禁红绿**，规避涨跌语义与五档状态色冲突），单元格带数值文本，图注声明"暖=正" |
| 6 | 持仓时间线 | plotly 水平条带 | 各资产段 categorical 色 + 段内文字标签，色永伴文字 |
| 7 | 调仓记录表 | `st.table` | 近 N 条：信号日/执行日/方向/成交价/溢价 |
| 8 | 对照区 | `st.table` | 双层（指数信号层 vs ETF 执行层）差异 + 约束统计（如 QDII 溢价跳过月数） |
| 9 | 页脚 | `st.caption` | 数据契约（derived 实时读取）+ 复现命令行 |

侧栏固定：层切换（信号层/执行层/对照，radio 显式字符串）。

### 策略页标准区块（2026-08-26 增补，首个应用：`app/faber_strategy.py`）

在回测页区块基础上的第三种页面：面向**已验收策略的实盘视角**，增加"当前信号状态"区（置顶）。区块自上而下：

| # | 区块 | 实现 | 要点 |
|---|---|---|---|
| 1 | 策略名 + 元信息行 | `st.header` + `st.caption` | 规则一句话 · 资产 · 参数 · 验收状态 |
| 2 | **当前信号状态**（策略页专属） | `st.metric` ×4 + `st.markdown` | 各资产距均线 %（delta 文字"均线上·持有/均线下·现金"）· 当前组合权重 · 下次调仓日（calendar 动态推导，禁止硬编码日期） |
| 3 | 指标摘要 | `st.metric` ×4 | 年化 / 最大回撤 / 夏普 / 距历史峰值 |
| 4 | 净值图 / 5 回撤图 | plotly | 同回测页纪律（信号层+执行层+基准，wash 填充） |
| 6 | 证据链摘要 | `st.table` | 主检验/敏感性/WF/执行层复测/holdout 实测/终局裁决逐行，实测与裁决分列（不许把验收写成检验通过） |
| 7 | 信号历史 | `st.dataframe` | 近 24 个月 ●/— 矩阵 |
| 8 | 页脚 | `st.caption` | 实盘 ETF 映射 + 溢价纪律 + 复现命令 |

### 模型页附属区块：IC 自证（2026-09-09 增补，首个应用：`app/valuation_dashboard.py` 底部）

挂在模型页五件套之后的**验证型附属区块**（CMV 会员 Correlations 页复刻）：回答"这套估值分到底有没有预测力"。不是第四种页面类型，不改动五件套结构。

| # | 区块 | 实现 | 要点 |
|---|---|---|---|
| 1 | IC 总表 | `st.table` | 全部参与综合分的卡片 + 综合分本身 × {未来1年, 未来3年} 的 Spearman 秩相关 IC + n + 覆盖区间；IC<0 = 便宜→未来收益高（期望方向） |
| 2 | 散点图 | plotly | 估值z（expanding 当时视角，无前视）vs 沪深300 月末价未来收益；蓝点 + 墨灰 OLS 趋势线（判读辅助，非预测） |
| 3 | 口径 caption | `st.caption` | expanding ≥50 个月才输出；月频非独立不做 p 值断言；样本量小的卡（综合分 3 年口径 n≈55）注明"结论仅供参考" |

---

## 三、色彩系统

### 3.1 评级语义色（状态色，五档专用）

| rating | 档名 | 色值 | 色块上文字 |
|---|---|---|---|
| 1 | 极低估 | `#0ca30c` | 白（底上对比 3.4:1，仅限大号粗体） |
| 2 | 低估 | `#0ca30c` @ 50% | 墨 `#0b0b0b`（50% 绿叠白底后白字不可读） |
| 3 | 公允 | `#fab219` | 墨（黄底白字仅 1.8:1，禁止白字） |
| 4 | 高估 | `#ec835a` | 墨（橙底白字仅 2.7:1，禁止白字） |
| 5 | 极高估 | `#d03b3b` | 白（对比 4.8:1） |

规则：**颜色永远伴随文字标签**（黄/橙浅表面对比度 <3:1 的既定缓解）；**当前档色块/徽章也按本表呈现**（当前档=2 时同样 50% 绿，勿因"当前"而用全值）；状态色禁止用作数据序列色；档名只用这五个词，不许自造同义词。

### 3.2 数据序列色

- 主序列统一 categorical 蓝 `#2a78d6`（单序列**不加图例**——标题即命名）。
- 多序列按 categorical 固定顺序取色（dataviz 参考色板，light 模式）：blue `#2a78d6` → orange `#eb6834` → aqua `#1baf7a` → yellow `#eda100` → magenta `#e87ba4` → green `#008300` → violet `#4a3aa7` → red `#e34948`。**按实体不按名次**，超过 8 个序列不生成第 9 色（折叠为"其他"或分面）。

### 3.3 图面 chrome

| 角色 | 色值 |
|---|---|
| 图表面板 | `#fcfcfb` |
| 主墨（标题/大字） | `#0b0b0b` |
| 次墨（副标题/参考线标注） | `#52514e` |
| muted（轴/次要标签） | `#898781` |
| 网格 hairline | `#e1e0d9` |

字体：`system-ui, -apple-system, 'Segoe UI', sans-serif`。大字数字用默认 proportional figures；仅表格行/轴刻度用 tabular-nums 对齐。

### 3.4 大面积区域

极值区/背景带只用 8% 透明度 wash：极便宜/低估侧 `rgba(12,163,12,0.08)`，极贵/高估侧 `rgba(208,59,59,0.08)`。饱和色禁止大面积铺——饱和色只给小标记和徽章。

---

## 四、图表规范

1. 数据线 2px；网格**实线** hairline（虚线会读成"预测/阈值"）。
2. 阈值与参考线允许虚线：均值线 `dash`、σ 界线 `dot`，标注用 muted 小字。
3. **禁止双轴**。两个量纲 → 两张图或小倍数图。
4. hover 用 `x unified` 模式；模板格式如 `%{x|%Y-%m}：ERP %{y:+.2f}pp`。
5. 文本（数值/标签/图例）一律用墨色，不用序列色；序列色只给线/点本身。
6. y 轴对称类图（估值z）范围对称，零线用次墨实线。

---

## 五、文案规范（白话解释模板）

```
{指标人话定义，含当前分子分母数值}。
按{窗口}口径（{起点} 起 {n} 个月，均值 {mean}、σ {sd}），
当前值偏离均值 {val_z:+.2f}σ，评级「{档名}」——{档位含义一句话}。
```

- 一段话 = 是什么 + 在哪档 + 意味着什么；数字全部从 MetricResult 填充，禁止手写死数。
- 逆向指标描述时明确"恐慌 = 机会区"式翻转语义，避免读者按直觉读反。
- 锚点回验的"事后"列必须标注为人工判读，不许写成模型的预测。

---

## 六、交互与数据纪律

1. 指标实时计算**不落盘**（AGENTS.md 规则 5）；`st.cache_data(ttl=3600)`。
2. 窗口切换用显式字符串选项（`'10y'`/`'full'`），radio 不放 `None` 值（测试工具与状态回放的坑）。
3. 新增模型两处注册：`src/metrics/` 加函数 + 页面 `MODELS` 注册表补一行。
4. expanding 副图样本 <50 月的时段输出 NaN 并在标题注明"样本不足不评级"，不插值不隐藏。
5. plotly 调用用 `width="stretch"`（新版 streamlit 已废弃 `use_container_width`）。

---

## 七、可复用组件（`app/components.py` 共用模块，勿在各页重写）

| 组件 | 位置 | 用途 |
|---|---|---|
| 色彩常量 | `components.INK/INK2/MUTED/GRID/SURFACE` + `CATEGORICAL`（8 色）+ `GREEN_WASH/RED_WASH` | 全部页面取色的唯一来源 |
| 通用图面 | `base_layout(fig, y_title)` | 面板色/网格/轴墨/hover 一次到位（showlegend 默认关，多序列页面自行开启） |
| 色值转 rgba | `hex_a(hex, alpha)` | 同色系降透明度 |
| 档位呈现色 | `tier_fill(rating)` / `tier_text(rating)` | 3.1 表的代码化：@50%、墨/白文字 |
| 五档色条 | `_tier_bar(m)`（仪表盘页） | 五件套第 3 件 |
| hero + 徽章 | 页面区第 2 块的 HTML（仪表盘页） | 五件套第 1+2 件 |
| 主图模板 | `_fig_erp(m)`（仪表盘页） | σ 带 + wash + 阈值线画法基准 |
| 副图模板 | `_fig_val_z(ez)`（仪表盘页） | expanding 演进画法基准 |

2026-08-24 起组件抽至 `app/components.py`（`browse_data.py` 对齐规范时触发，早于第 4 步多卡片）；页面私有模板留在各自页面，扩卡片时再上移。

---

## 八、变更流程

与 AGENTS.md 同律：改本规范先改本文件并说明理由 → 再动代码；每完成一处前端工作，`PROGRESS.md` 当场记录。风格漂移（新页面颜色/结构/文案与本文件不一致）视为 bug。
