# 架构说明

> 本文档说明项目当前的文件结构、各部分的用途和相互关系。
> 设计决策的"为什么"见 `指数ETF量化系统设计方案.md`；Agent 工作纪律见 `AGENTS.md`；做到哪了见 `PROGRESS.md`。本文档只回答"这些文件是什么、怎么连起来的"。
> 本文更新至 2026-08-26（第 1–3 步完成、第 4 步推进中：策略页 + 7/12 卡片 + 综合分 + 快照）。

## 一、全景图

```text
              ┌──────────┐
              │ AKShare  │  外部数据源（新浪/东财/腾讯/中证/乐咕…）
              └────┬─────┘
                   │ 抓取（重试 + 兜底链）
                   ▼
   ┌────────────────────────────┐
   │ src/data_module/（数据模块） │  net → sources → update
   │                            │  storage（落盘规范，全员共用）
   └─────────────┬──────────────┘
                 │ 写入（raw 只增不改）
                 ▼
   ┌────────────────────────────┐
   │ data/raw/（42 parquet       │── validate 五查 / tracking_check ──► reports/
   │ + 42 .meta.json）           │── bond_splice ──► derived/aligned/
   └─────────────┬──────────────┘
                 │ 读取
     ┌───────────┴────────────────────────────┐
     ▼                                        ▼
 ┌────────────────────┐              ┌────────────────────────┐
 │ src/strategy/      │              │ src/metrics/（指标层）   │
 │ 策略层              │              │ σ 引擎 + 卡片 + 综合分   │
 │ panels → 信号       │              │ 实时计算不落盘           │
 │   → engine 回测     │              │ （snapshot 例外，见下）  │
 │   → sweep / WF      │              └───────────┬────────────┘
 └─────────┬──────────┘                          │ 每日追加一行
           │ 写 derived                          ▼
           ▼                          data/derived/metrics_history.parquet
 ┌──────────────────────────────────────────────┐
 │ data/derived/（可删可重建）                     │
 │   signals/ 月度信号表 · backtests/ 回测净值     │
 │   aligned/ 债券腿拼接 · metrics_history 快照   │
 └──────────────────┬───────────────────────────┘
                    │ 读取（页面不做重计算）
                    ▼
   ┌────────────────────────────┐
   │ app/（Streamlit 展示层）     │  统一入口 paseo.py，侧栏切四页
   └────────────────────────────┘
```

数据流向单向：**AKShare → data_module → raw →（strategy/metrics → derived）→ app**。没有任何组件回写 raw。

## 二、目录与文件清单

### 文档层（项目根目录）

| 文件 | 用途 |
|---|---|
| `指数ETF量化系统设计方案.md` | **基准设计文档**：策略方向、工具栈、标的池、数据清单、执行/验证纪律、第六节估值方法、第十/十一节基线定稿。一切决策以它为准 |
| `DESIGN.md` | **前端设计规范**（CMV 风格定稿）：页面骨架五件套、五档语义色、图表/文案纪律、可复用组件、回测页/策略页区块类型。所有前端页面以此为准，风格漂移视为 bug |
| `AGENTS.md` | Agent 工作约定（硬性纪律），每次会话自动加载 |
| `PROGRESS.md` | 进展记录：做到哪、遗留事项、常用命令、踩坑。跨会话接力先读它 |
| `架构说明.md` | 本文档 |
| `pyproject.toml` / `uv.lock` | uv 项目定义与依赖锁（Python 3.12：akshare/pandas/pyarrow/streamlit/plotly/vectorbt/bt/quantstats/optuna） |
| `.venv/` | uv 虚拟环境（勿手动改） |

### 代码层

#### `src/data_module/` —— 数据模块（第 1 步交付物，已验收）

| 模块 | 职责 | 被谁用 |
|---|---|---|
| `net.py` | 网络底层：东财域名补丁 + 指数退避重试（`retry_fetch`） | sources |
| `sources.py` | **数据集注册表**：数据集定义（名称/路径/抓取函数/兜底链）。新增数据只改这里 | update |
| `storage.py` | **落盘规范的唯一入口**：列名标准化（date/open/high/low/close/volume/amount）、增量追加（旧行优先=只增不改）、`.meta.json` 侧车读写 | sources、update、validate、strategy、metrics 全员 |
| `update.py` | CLI 入口：全量/按名增量更新，断点续传 | 人/定时任务/数据浏览器按钮 |
| `validate.py` | 数据验收五查（只读） | 人 |
| `bond_splice.py` | 债券腿拼接：511260 上市前接中债国债财富指数，**收益率对齐**，产出到 derived | 人/策略 |
| `tracking_check.py` | ETF 对指数的长期跟踪差检验（只读 raw，写 `reports/`） | 人 |

依赖关系：`net → sources → update`，`storage` 被所有模块共用；`validate`/`bond_splice`/`tracking_check` 互相独立，只读 raw。

#### `src/strategy/` —— 策略层（第 2、3 步交付物，已完结：4 策略 1 通过 3 放弃）

| 模块 | 职责 | 被谁用 |
|---|---|---|
| `contract.py` | **统一契约**：StrategyConfig（承载"不改一个数"的原参数）+ BacktestResult（净值/持仓/调仓/摘要）。与指标层 MetricResult 平行 | 全模块 |
| `panels.py` | **通用信号面板**：资产键→数据源映射（信号层干净价格）、A股交易日骨架（长假错位教训）、cash 列=中债 3M 利率复利（替代旧"固定 2%"约定）、月末锚点（带新鲜度截断防未来伪信号） | gem_signal、baseline_signals |
| `gem_signal.py` | GEM 双动量信号：月末比较标普500(×汇率)与沪深300 的 12M 回报选强者，跑不赢 3M 国债复利则转债券防御 | run_gem |
| `baseline_signals.py` | 三基线信号：Faber 10月均线 / FED 分位 / GTAA 动量轮动。统一输出月度信号表 + 执行日权重表 | run_baselines |
| `engine.py` → `engine_pandas.py` | 回测引擎统一入口；**pandas 主管道**（精确口径 + 费用建模 + 无未来函数约定：区间收益记给该区间持仓） | run_gem、run_baselines |
| `engine_bt.py` | bt 组合引擎：工具栈合规 + 与主管道对账（测试 <1e-4），非主管道 | 测试 |
| `engine_weights.py` | 组合权重版引擎（多资产，单资产切换为其特例，测试恒等 0e+00） | run_baselines |
| `qdii_filter.py` | QDII 溢价代理与买入门控（63 日滚动锚定拆分断崖，仅执行层） | run_gem |
| `run_gem.py` | GEM 两层编排：信号层（指数 2008 起）/ 执行层（ETF qfq 2014 起，含溢价门控） | backtest CLI |
| `run_baselines.py` | 三基线编排 + CLI + 判读表（通过线自动判读） | 人 |
| `backtest.py` / `sweep.py` / `walkforward.py` | CLI：GEM 两层回测 / 回望敏感性扫描 / Faber WF+holdout 终验 | 人 |
| `report.py` | QuantStats HTML 报告 + 摘要（失败时退化自产 HTML） | backtest |

依赖关系：`contract + panels` 为地基；信号模块（gem_signal/baseline_signals）→ 引擎（engine*）→ 编排（run_*）→ CLI（backtest/sweep/walkforward）。`qdii_filter` 只被执行层引用，不污染信号层。

#### `src/metrics/` —— 指标层（估值体系，CMV 复刻）

| 模块 | 职责 | 被谁用 |
|---|---|---|
| `contract.py` | **统一契约**：MetricResult（name/current/unit/sigma/rating/color/series/±1σ带/desc/direction）+ 五档评级表 + 方向归一 | 所有卡片、仪表盘 |
| `sigma.py` | **σ 评估引擎**（设计文档第六节）：月频采样、双窗口、expanding 当时视角（无前视，≥50 月才评级） | 所有卡片 |
| `erp.py` | 股债利差 ERP（FED 模型）：1/PE − 10Y 国债，中间量同结构保留 | 仪表盘 |
| `curve_10y1y.py` | 10Y−1Y 曲线利差（INVERSE：倒挂=风险区） | 仪表盘 |
| `dividend_spread.py` | 全A股息率 − 10Y（口径声明：300 股息率长历史缺失，以全 A 替代） | 仪表盘 |
| `more_cards.py` | 4 张卡片模板 `_card`：300 PE 分位 / 250 日均线偏离 / 10Y 水平 / QVIX 情绪 | 仪表盘 |
| `aggregate.py` | σ 加权综合分（当前 7 卡等权，诚实声明同源相关性）；历史序列 = 各卡 expanding 按月对齐平均 | 仪表盘默认卡 |
| `snapshot.py` | 每日评级快照 CLI：算 7 卡 + 综合，按数据截止日追加 `metrics_history.parquet`（同日幂等） | 人/定时任务 |

指标结果**不落盘**：仪表盘实时计算 + `st.cache_data` 缓存（AGENTS.md 规则 5，唯一例外 `metrics_history.parquet`）。新增估值模型 = 在 `erp.py` 旁加一个函数 + 仪表盘 `_MODEL_SPECS` 函数表补一行。

#### `app/` —— 展示层（Streamlit）

| 文件 | 用途 |
|---|---|
| `app/paseo.py` | **统一入口**：`st.navigation` 侧栏切四页，页面文件原位保留（AppTest 仍直指原文件） |
| `app/components.py` | DESIGN.md 的代码化：色彩常量（墨色系 chrome / categorical 8 色）+ `base_layout` + `tier_fill`/`tier_text`。全部页面唯一取色来源，页面内禁止重写 |
| `app/faber_strategy.py` | Faber 策略页（默认页，实盘视角）：当前信号状态（距均线/权重/下次调仓日动态推导）+ 证据链表（实测与裁决分离）+ 近 24 月信号矩阵 + 实盘 ETF 映射页脚 |
| `app/valuation_dashboard.py` | 估值仪表盘：综合卡（默认）+ 7 模型卡，每卡五件套（当前值大字/偏离σ/五档红绿灯/走势+σ带/白话解释）+ expanding 评级演进 + 双窗口 + 锚点回验 |
| `app/gem_backtest.py` | 回测工作台：GEM 两层 / 敏感性扫描 / 基线三策略判读表 / 四策略净值对比。只读 derived 落盘结果（页面不做回测） |
| `app/browse_data.py` | 数据浏览器：总览表（中文名/行数/区间/来源）+ 走势图 + 明细；侧栏数据更新按钮（带锁文件并发保护、部分失败话术、子进程 PYTHONPATH）。中文名登记在文件顶部 `DATASET_NAMES` |

#### `scripts/` —— 测试与一次性脚本（不进主流程）

| 文件 | 用途 |
|---|---|
| `test_gem_signal.py` / `test_gem_backtest.py` | GEM 信号与回测回归（含 sweep 一致性 + vbt 交叉断言） |
| `test_baselines.py` | 三基线回归（引擎特例恒等/扰动不变性/权重合法性/Faber 锚点/两层一致性） |
| `test_gem_backtest_app.py` / `test_faber_strategy_app.py` / `test_frontend_alignment.py` | AppTest 页面冒烟与设计规范断言 |
| `sweep_baselines.py` | 三基线敏感性扫描（Faber SMA 网格 / FED 阈值×分位窗 / GTAA TopN×回望） |
| `probe_interfaces*.py` / `probe_backtest_libs.py` | AKShare 接口探针与三回测库冒烟，留档备查 |
| `fill_n225_em.py` | 东财恢复后为日经225 补长历史的专用脚本 |

### 数据层

```text
data/
├── raw/                      # 原始层：只增不改
│   ├── index_daily/          # A股指数日线 ×5（信号层）
│   ├── index_global/         # 海外指数 ×3 + HSHYLV 净值兜底（信号层）
│   ├── etf_daily/            # 12 只 ETF 前复权日线（执行层）
│   ├── etf_nav/              # 4 只 QDII 净值（溢价过滤器数据源）
│   ├── valuation/            # PE/PB ×4 + 股息率 ×3
│   ├── rates/                # 中债国债收益率曲线（3M–30Y 全期限 9 列）
│   ├── qvix/  fx/  gold/  bond_index/
│   └── calendar.parquet      # A股交易日历（锚点→执行日推导）
│   （每个 parquet 配一个同名 .meta.json：来源接口/复权方式/抓取日期/备注）
│
└── derived/                  # 派生层：由 raw 全量重算，可删可重建
    ├── aligned/              #   债券腿拼接净值 bond_leg_spliced_511260.parquet
    ├── signals/              #   月度信号表 {faber,fed,gtaa,gem_cn}_monthly.parquet
    ├── backtests/            #   回测净值/交易明细/sweep/判读表（faber_etf 等 ETF 层）
    └── metrics_history.parquet  # 每日评级快照（唯一允许追加写的文件）
```

### 报告层

| 文件 | 用途 |
|---|---|
| `reports/tracking_check.md` | 跟踪差检验报告（`tracking_check.py` 生成） |
| `reports/gem_cn_{index,etf}.html` | GEM 两层 QuantStats 报告（`strategy.backtest` 生成，基准=沪深300） |

## 三、关键约定（改代码前必读）

1. **raw 只增不改**：追加逻辑在 `storage.append_save`（按 date 去重、旧行优先）。想换数据源必须删文件重抓；扩列同样要删了重抓。
2. **derived 全量重算**：`read raw → compute → overwrite`，不做增量，随时可删。唯一例外：`metrics_history.parquet` 允许每日追加一行（同日重跑幂等覆盖）。
3. **新增一个数据集的三处改动**：`sources.py` 注册抓取函数（必须）→ `browse_data.py` 的 `DATASET_NAMES` 补中文名（展示）→ `PROGRESS.md` 记录（纪律）。
4. **新增一张估值卡**：`metrics/` 加函数（复刻 erp.py 模式走 σ 引擎）→ 仪表盘 `_MODEL_SPECS` 补一行 → `test_frontend_alignment.py` 补遍历。
5. **页面只读不算**：回测由 CLI 产出落盘 derived，页面读落盘结果；实时信号（如 Faber 当前状态）例外，走 `@st.cache_data`。
6. **无未来函数**：信号次日开盘成交；锚点日期做数据新鲜度截断（防月末伪信号）。

## 四、页面入口与运行节奏

```bash
uv run streamlit run app/paseo.py   # 统一入口：Faber 策略 / 估值仪表盘 / 回测工作台 / 数据浏览器
```

日常节奏：每日 `data_module.update` + `metrics.snapshot`；月末最后交易日收盘后跑 `strategy.run_baselines` 刷新 Faber 信号，次月首个交易日开盘按信号调仓。（完整命令清单见 `PROGRESS.md`「常用命令」。）

## 五、当前状态与余量

- 第 1–3 步完结：数据模块验收 5/5；四基线终局 **Faber 唯一通过**（WF ✅ / holdout 差幅小 → 用户裁决通过），GEM/FED/GTAA 判结构性失败，留作反面参照（代码与卡片页保留，不进实盘候选）。
- 第 4 步推进中：Faber 策略页 ✅、估值卡片 7/12 + 综合分 ✅、快照机制 ✅（首行已写入）。
- 余量（按价值排序）：① 估值分 vs 未来收益散点（`composite_series` 已可回算，仪表盘预测力自证）；② 剩余 5 卡需新数据源（巴菲特指标/两融/涨跌停情绪等，先探 akshare 接口）；③ 2027 年 Faber 新 holdout 追加确认（建议项，非纪律）。
